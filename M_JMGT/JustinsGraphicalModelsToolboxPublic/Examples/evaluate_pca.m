function [hor_efeat_ij ver_efeat_ij] = evaluate_pca(input)

if isempty(efeat)
    npairs = size(model.pairs,1);
    efeat  = ones(npairs,1);
end

x = feat;
y = zeros(model.nnodes,1);
z = efeat;
        
end